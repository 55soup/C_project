//#include<stdio.h>//#include<Windows.h>//#include<time.h>////int coin_toss()//{//	int i = rand() % 2; //0?1//	if (i == 0)return 0;//뒷면//	else return 1;//앞면//}//main()//{//	srand((unsigned)time(NULL));//	int toss;//	int head = 0;//	int tail = 0;//	for (toss = 0; toss < 10; toss++)//	{//////		if (coin_toss() == 1) {//			head++;//		}//		else if (coin_toss() == 0) {//			tail++;//		}//	}////	printf("동전의 앞면 %d\n", head);//	printf("동전의 뒷면 %d\n", tail);//	system("pause");//}
